server:
  port: 8081

spring:
  application:
    name: user-service

  config:
    import: optional:configserver:${CONFIG_SERVER_URL}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}